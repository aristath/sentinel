# GET /api/version

Get application version.

**Description:**
Returns the current version of the Sentinel application.

**Request:**
- Method: `GET`
- Path: `/api/version`
- Parameters: None

**Response:**
- Status: `200 OK`
- Body:
  ```json
  {
    "version": "1.0.0"
  }
  ```
