# GET /api/trades/recommendations

Get current trade recommendations.

**Description:**
Returns the current trade plan/recommendations generated by the planning system. Includes recommended trades with scores and feasibility information.

**Request:**
- Method: `GET`
- Path: `/api/trades/recommendations`
- Parameters: None

**Response:**
- Status: `200 OK`
- Body: Trade plan object with steps:
  ```json
  {
    "steps": [
      {
        "action": "BUY",
        "symbol": "AAPL.US",
        "quantity": 10.0,
        "score": 0.85,
        "reason": "Opportunity buy"
      }
    ]
  }
  ```

**Error Responses:**
- `500 Internal Server Error`: Service error


### Trade Validation Endpoints

All trade validation endpoints help validate trades before execution without actually executing them.
